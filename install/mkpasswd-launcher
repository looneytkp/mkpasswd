#!/usr/bin/env pwsh
# vaultpass.ps1 - PowerShell launcher for vaultpass

$script = "$HOME\.vaultpass\core\vaultpass"

if (-Not (Test-Path $script)) {
    Write-Host "[!] vaultpass core script not found at $script"
    exit 1
}

bash $script @args